import { CallOnInitDirective } from './call-on-init.directive';

describe('CallOnInitDirective', () => {
  it('should create an instance', () => {
    const directive = new CallOnInitDirective();
    expect(directive).toBeTruthy();
  });
});
