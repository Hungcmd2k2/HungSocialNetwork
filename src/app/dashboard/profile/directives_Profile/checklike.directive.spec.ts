import { ChecklikeDirective } from './checklike.directive';

describe('ChecklikeDirective', () => {
  it('should create an instance', () => {
    const directive = new ChecklikeDirective();
    expect(directive).toBeTruthy();
  });
});
